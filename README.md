# A repository containing some autohotkey script I use

### no-alt-codes
Replaces the Numpad keys with the regular (non-numpad) version of the keys to avoid triggering the alt codes (ASCII) when NumLock is off.
